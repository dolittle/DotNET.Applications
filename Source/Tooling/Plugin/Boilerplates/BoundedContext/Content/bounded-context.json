{
  "application": "{{applicationId}}",
  "boundedContext": "{{createGuid}}",
  "boundedContextName": "{{name}}",
  "resources": {
    "readModels": {
      "production": "{{readModelsImplementation}}",
      "development": "{{readModelsImplementation}}"
    },
    "eventStore": {
      "production": "{{eventStoreImplementation}}",
      "development": "{{eventStoreImplementation}}"
    }
  },
  "core": {
    "language": "csharp"
  },
  "interaction": []
}